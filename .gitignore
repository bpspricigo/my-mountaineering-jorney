config.js
.cache/
.mcp.json

# The worldwide peak file is an upload artifact, not source: tens of megabytes,
# regenerated by scripts/build-world-peaks.mjs and hosted by MapTiler.
# data/peaks-core.geojson, the few thousand peaks the app needs offline, is
# committed.
data/world-peaks.geojson
